import React from 'react';
import AddedProducts from '@/components/AddedProducts';
const Page = () => {
    return (
        <div>
            <AddedProducts />
        </div>
    );
}

export default Page;
