import ShoppingCart from '@/components/ShoppingCart';
import React from 'react';

const Page = () => {
    return (
        <div>
            <ShoppingCart/>
        </div>
    );
}

export default Page;
